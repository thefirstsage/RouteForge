# Assets

This folder is reserved for future product assets such as a custom app icon, installer artwork, screenshots, or brand graphics.

The current review build uses the default PyInstaller executable icon.
